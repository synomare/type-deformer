# Known limitations — 0.2.0-preview.1

- 動作保証は現行Desktop Chrome／Edgeです。Safari／Firefox／スマートフォンはbest effortです。
- 動画出力はWebCodecs対応のChrome／Edge専用です。codec、解像度、端末の実装によってMP4またはWebMを生成できない場合があります。失敗時に形式は自動変更しません。
- 390px／320pxはレイアウト崩れと横overflowを検査しますが、スマートフォンを正式対応環境とはしません。
- SVGで`SVG内Raster`と表示されるOperatorは、効果部分を埋め込み画像として保持します。編集先にも同じフォントがない場合、text要素の見た目が変わります。
- Google Fontsを外部から読み込みます。オフライン、通信制限、配信側障害では代替フォントになり、見た目や改行が変わる場合があります。
- Camera入力はHTTPSまたはlocalhostと利用者の許可が必要です。Camera映像は端末内で処理します。
- Autosaveは同じブラウザの保存領域に依存します。重要な作品はProjectファイルも保存してください。
- `noindex`は検索エンジンへの指示であり、URLを知る人の閲覧や共有を防ぐアクセス制御ではありません。

## フォントの読み込み

- 1ファイルのImportはそのまま適用、複数ファイル／フォルダは検索できるLibraryへ登録し、選択した書体だけを読み込みます。選択したフォントファイルはサーバーへ送信しません。
- iPhoneではFiles内のOTF／TTF／WOFF／WOFF2等を選択してください。ZIPは先に展開し、クラウド内だけのファイルは端末へダウンロードしてから選択すると再試行できます。空データ・読取失敗・書体デコード失敗は画面で区別します。
- TTC／OTCコレクションは先頭書体を取り出して利用します。2書体目以降が必要な場合は単体OTF／TTFを選択してください。コレクションの全書体選択には未対応です。
- Libraryのフォントデータはタブ内のメモリに保持します。再読込・ブラウザ終了後は同じファイルを再Importしてください。Project／Share URLにはフォントの実データを含めません。
- Installed fontsはLocal Font Access対応ブラウザでのみ表示します。選択した書体のデータを読み込み、Workerと書き出しにも渡します。
- WOFF2の表示は可能ですが、Axis Field用の可変軸解析は未対応です。可変軸の操作には可変TTF／OTF／WOFFを使ってください。
- WebKitでフォント読込と描画の回帰を検査していますが、iPhone実機のFiles／iCloud連携は未検証です。
